
%镜像
hold on
   plot(-coordinates(:,1), -coordinates(:,2));
mirror only in x axis

   hold on
       plot(coordinates(:,1), -coordinates(:,2));
mirror only in y axis

   hold on
       plot(-coordinates(:,1), coordinates(:,2));
*
